# sleek CI — merge builds + Radicle issue → cursor-agent → patch.
#
# Loaded from repo checkout by boxci (webhooks /api/webhooks/garden*, /api/poll).
# Reuses scripts/ci-nixbuild.sh + nix .#android / .#flatpak (same as Buildkite/GHA).
#
# Server sets: BOXCI_REPO_ROOT, BOXCI_REPO_URL, GIT_SHA, GIT_BRANCH, BOXCI_TRIGGER
# Issue/poll also set RADICLE_TRIGGER + RADICLE_ISSUE_ID (Buildkite script compat).

name: sleek
on:
  - merge
  - issue
  - poll

env:
  RUST_BACKTRACE: "1"
  GIT_TERMINAL_PROMPT: "0"

steps:
  - label: ":rust: Check"
    key: check
    if: build.env("BOXCI_TRIGGER") == "merge"
    command: |
      set -euo pipefail
      cd "${BOXCI_REPO_ROOT:?}"

      if command -v apt-get >/dev/null 2>&1; then
        sudo DEBIAN_FRONTEND=noninteractive apt-get update
        sudo DEBIAN_FRONTEND=noninteractive apt-get install -y jq git patch curl ca-certificates
      fi
      command -v jq >/dev/null 2>&1 || { echo "jq required" >&2; exit 1; }

      if ! command -v nix >/dev/null 2>&1; then
        curl --proto '=https' --tlsv1.2 -sSf -L \
          https://install.determinate.systems/nix \
          | sh -s -- install linux --no-confirm --init none
        # shellcheck disable=SC1091
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh || true
        export PATH="/nix/var/nix/profiles/default/bin:$PATH"
      fi
      if [[ ! -S /nix/var/nix/daemon-socket/socket ]]; then
        sudo /nix/var/nix/profiles/default/bin/nix daemon >/tmp/nix-daemon-check.log 2>&1 &
        for _ in $(seq 1 30); do
          [[ -S /nix/var/nix/daemon-socket/socket ]] && break
          sleep 1
        done
      fi

      sudo mkdir -p /etc/nix
      if ! sudo grep -q 'accept-flake-config' /etc/nix/nix.conf 2>/dev/null; then
        echo 'accept-flake-config = true' | sudo tee -a /etc/nix/nix.conf >/dev/null
      fi
      export NIX_CONFIG=$'experimental-features = nix-command flakes\naccept-flake-config = true'

      bash scripts/sync-flake-path-deps.sh
      parent="$(dirname "$PWD")"
      [[ -f "$parent/vidya/Cargo.toml" ]] || { echo "vidya Cargo.toml missing" >&2; exit 1; }
      [[ -f "$parent/freeq/freeq-sdk/Cargo.toml" ]] || {
        echo "freeq-sdk Cargo.toml missing" >&2
        exit 1
      }

      echo "--- :nix: develop (mold + rustc from flake)"
      nix develop --option accept-flake-config true --command bash -lc '
        set -euo pipefail
        command -v mold >/dev/null
        rustc --version
        cargo --version
        cargo clippy --manifest-path host/Cargo.toml -- -D warnings
        cargo test --manifest-path android/Cargo.toml --lib
      ' </dev/null

  - label: ":android: APK (nixbuild.net)"
    key: apk
    if: build.env("BOXCI_TRIGGER") == "merge"
    depends_on: check
    allow_dependency_failure: true
    command: |
      set -euo pipefail
      cd "${BOXCI_REPO_ROOT:?}"
      slug="${BOXCI_REPO_SLUG:-sleek}"
      artifacts="${BOXCI_ROOT:?}/artifacts/${slug}/${BOXCI_RUN_ID:?}"
      mkdir -p "$artifacts"

      chmod +x scripts/ci-nixbuild.sh scripts/fetch-openbao-env.sh

      creds_out="$(mktemp)"
      ./scripts/ci-nixbuild.sh resolve-creds | tee "$creds_out"
      # shellcheck disable=SC1090
      eval "$(grep -E '^(SKIP|NIXBUILD_CREDS_ENV)=' "$creds_out")"
      if [[ "${SKIP:-1}" == "1" ]]; then
        echo "--- :warning: Skipping APK (no nixbuild credentials)"
        exit 0
      fi
      # shellcheck disable=SC1090
      source "$NIXBUILD_CREDS_ENV"

      setup_out="$(mktemp)"
      ./scripts/ci-nixbuild.sh setup | tee "$setup_out"
      setup_env="$(sed -n 's/^NIXBUILD_SETUP_ENV=//p' "$setup_out" | tail -1)"
      [[ -n "$setup_env" && -f "$setup_env" ]] || {
        echo "ci-nixbuild setup did not emit NIXBUILD_SETUP_ENV" >&2
        exit 1
      }
      # shellcheck disable=SC1090
      source "$setup_env"

      ./scripts/ci-nixbuild.sh remote-build ".#android" result-android sleek.apk
      [[ -s result-android/sleek.apk ]] || {
        echo "APK missing under result-android/" >&2
        ls -la result-android >&2 || true
        exit 1
      }
      cp --reflink=auto result-android/sleek.apk "$artifacts/sleek.apk"
      ls -lh "$artifacts/sleek.apk"
      echo "artifact=$artifacts/sleek.apk"

  - label: ":package: Flatpak (nixbuild.net)"
    key: flatpak
    if: build.env("BOXCI_TRIGGER") == "merge"
    depends_on: check
    allow_dependency_failure: true
    command: |
      set -euo pipefail
      cd "${BOXCI_REPO_ROOT:?}"
      slug="${BOXCI_REPO_SLUG:-sleek}"
      artifacts="${BOXCI_ROOT:?}/artifacts/${slug}/${BOXCI_RUN_ID:?}"
      mkdir -p "$artifacts"

      chmod +x scripts/ci-nixbuild.sh scripts/fetch-openbao-env.sh

      creds_out="$(mktemp)"
      ./scripts/ci-nixbuild.sh resolve-creds | tee "$creds_out"
      # shellcheck disable=SC1090
      eval "$(grep -E '^(SKIP|NIXBUILD_CREDS_ENV)=' "$creds_out")"
      if [[ "${SKIP:-1}" == "1" ]]; then
        echo "--- :warning: Skipping Flatpak (no nixbuild credentials)"
        exit 0
      fi
      # shellcheck disable=SC1090
      source "$NIXBUILD_CREDS_ENV"

      setup_out="$(mktemp)"
      ./scripts/ci-nixbuild.sh setup | tee "$setup_out"
      setup_env="$(sed -n 's/^NIXBUILD_SETUP_ENV=//p' "$setup_out" | tail -1)"
      [[ -n "$setup_env" && -f "$setup_env" ]] || {
        echo "ci-nixbuild setup did not emit NIXBUILD_SETUP_ENV" >&2
        exit 1
      }
      # shellcheck disable=SC1090
      source "$setup_env"

      ./scripts/ci-nixbuild.sh remote-build ".#flatpak" result-flatpak \
        uk.nandi.sleek.flatpak
      bundle="result-flatpak/uk.nandi.sleek.flatpak"
      [[ -s "$bundle" ]] || {
        echo "Flatpak bundle missing under result-flatpak/" >&2
        ls -lah result-flatpak >&2 || true
        exit 1
      }
      cp --reflink=auto "$bundle" "$artifacts/uk.nandi.sleek.flatpak"
      ls -lh "$artifacts/uk.nandi.sleek.flatpak"
      echo "artifact=$artifacts/uk.nandi.sleek.flatpak"

  # --- Radicle issue poll (schedule / POST /api/poll) ---
  - label: ":radicle: Poll issues → dispatch agent"
    key: radicle-issue-poll
    if: build.env("BOXCI_TRIGGER") == "poll"
    command: bash scripts/boxci/poll-issues.sh

  # --- Radicle issue agent (webhook or poll dispatch) ---
  - label: ":radicle: Issue → :cursor: agent → patch"
    key: radicle-issue-agent
    if: build.env("BOXCI_TRIGGER") == "issue" || build.env("RADICLE_ISSUE_ID") != null
    command: bash scripts/buildkite/run-issue-agent.sh

  - label: ":mag: Verify patch (optional)"
    key: verify-patch
    if: build.env("BOXCI_TRIGGER") == "issue" || build.env("RADICLE_ISSUE_ID") != null
    depends_on: radicle-issue-agent
    allow_dependency_failure: true
    command: |
      set -euo pipefail
      cd "${BOXCI_REPO_ROOT:?}"
      BRANCH="${RADICLE_ISSUE_BRANCH:-}"
      if [[ -z "$BRANCH" ]]; then
        if [[ -n "${RADICLE_ISSUE_ID:-}" ]]; then
          BRANCH="issue/${RADICLE_ISSUE_ID:0:7}"
        fi
      fi
      if [[ -z "$BRANCH" ]]; then
        echo "No issue branch — prior step likely skipped."
        exit 0
      fi
      if git show-ref --verify --quiet "refs/heads/$BRANCH"; then
        echo "Branch $BRANCH exists."
        git log -1 --oneline "$BRANCH"
      else
        echo "Branch $BRANCH not found — agent may have declined or failed before branching."
        exit 0
      fi
      echo ""
      echo "Open patches:"
      rad patch list --open 2>/dev/null | sed -n '1,15p' || rad patch 2>/dev/null | sed -n '1,15p' || true
